import 'package:flutter/material.dart';

class PlayCover extends StatelessWidget {
  const PlayCover({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
